export interface ProgramCategory {
  id: number;
  name: string;
  image: string;
}

export interface ProgramListItem {
  id: number;
  name: string;
  image: string;
}

export interface ProgramDetails {
  id: number;
  name: string;
  description: string;
  image: string;
  price: number;
  week: number;
}
